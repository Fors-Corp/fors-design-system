---
"@marcfs31/fors-design-system": patch
---

Bump tailwind-merge (production dependency).

- `tailwind-merge`: 3.6.0 -> 3.7.0

Added by the dependabot-codeql-triage task: this bump touches "dependencies"
(not devDependencies/peerDependencies) and would otherwise ship with no
changelog entry once merged. The Dependabot auto-merge workflow normally adds
this automatically, but its job has been skipped on every PR since the
repository was transferred to the Fors-Corp org (its `github.repository`
guard still names the old path).
